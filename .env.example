# Outreachly — environment template. Copy to `.env` and fill in. NEVER commit `.env`.
# Only external-IO providers need keys. ICP generation, copywriting and reply
# classification are done by Claude when the skill runs — no LLM key required here.

# --- Unipile: LinkedIn discovery/DM, Email send, unified inbox ---
# https://developer.unipile.com/  (DSN is your workspace base URL incl. port)
UNIPILE_DSN=
UNIPILE_API_KEY=

# --- X (Twitter) API v2 ---
# Discovery/search needs only the app bearer token:
X_BEARER_TOKEN=
# DM sending needs OAuth 1.0a user context (app set to Read+Write+DM, then generate user tokens):
X_API_KEY=
X_API_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_SECRET=

# --- Hunter.io: email finding ---
# https://hunter.io/api-keys
HUNTER_API_KEY=

# --- NeverBounce: email verification ---
# https://app.neverbounce.com/settings/api
NEVERBOUNCE_API_KEY=

# --- Optional ---
# DEFAULT_LOCALE=en        # en | zh
# OUTREACHLY_DB=./data/crm.sqlite
