# Outreachly skill — dependencies kept minimal.
#   httpx       — external API calls
#   jsonschema  — validate ICPs against reference/icp_schema.json
# Everything else (sqlite3, http.server, argparse, json) is the Python standard library.
httpx==0.28.1
jsonschema==4.23.0

# Dev only:
pytest==8.3.4
