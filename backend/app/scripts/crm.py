"""CRM CRUD: insert with dedup, ICP match scoring, lead status state machine (P2)."""

from __future__ import annotations

from app.scripts._stub import stub_main


def main() -> int:
    return stub_main("crm", "P2", "CRM CRUD: insert with dedup, ICP match scoring, lead status state machine")


if __name__ == "__main__":
    raise SystemExit(main())
