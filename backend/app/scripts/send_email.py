"""Send emails via Unipile (Gmail/Outlook) with scheduling and throttling (P4)."""

from __future__ import annotations

from app.scripts._stub import stub_main


def main() -> int:
    return stub_main("send_email", "P4", "Send emails via Unipile (Gmail/Outlook) with scheduling and throttling")


if __name__ == "__main__":
    raise SystemExit(main())
