"""Pull replies from the unified inbox, classify intent, update CRM (P4)."""

from __future__ import annotations

from app.scripts._stub import stub_main


def main() -> int:
    return stub_main("reply_handler", "P4", "Pull replies from the unified inbox, classify intent, update CRM")


if __name__ == "__main__":
    raise SystemExit(main())
