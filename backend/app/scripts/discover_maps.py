"""Discover leads via Google Places API and write them to the CRM (P3)."""

from __future__ import annotations

from app.scripts._stub import stub_main


def main() -> int:
    return stub_main("discover_maps", "P3", "Discover leads via Google Places API and write them to the CRM")


if __name__ == "__main__":
    raise SystemExit(main())
